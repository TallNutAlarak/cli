//  <%= data.description %>  ---start
export interface <%= data.IParamsName %>{
    <% data.IParams.forEach(function(item){%><%=item.field %>: <%=item.type %>;
    <% }) %>
}
<%= data.IRet %>
export const <%= data.name %> = (data:<%= data.IParamsName %>) => {
    return request({
        url: '<%= data.url %>',
        method:'<%= data.method %>',
        data,
    }).catch(err=>{
        const errCode = err.response.data.code;
        switch(errCode){<% data.error.forEach(function(item){%>
                case <%=item.code %>: message.error("<%=item.message %>")
            <% }) %>
        }
    }) as Promise<AxiosResponse<<%= data.IRetName %>>>;
};
//  <%= data.description %> ----end


